# Kickoff Prompt — setup-meta-ads

Paste this into Claude Code to begin the guided setup:

---

Read the skill at `./skills/meta-ads/setup-meta-ads/SKILL.md` and walk me
through it step by step to set up my Meta Ads CLI.

Before we start:
1. Check whether meta-ads is installed: `pip install meta-ads`
2. Help me create a `.env` from `.env.example` — I will need `ACCESS_TOKEN`
   and `AD_ACCOUNT_ID`

I will follow your lead — tell me exactly what to run and what to look for
at each step.

---
